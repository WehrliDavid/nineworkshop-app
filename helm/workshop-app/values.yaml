replicaCount: 2

image:
  repository: docker-agi.355592e.registry.nineapis.ch/nineworkshop-app
  tag: "latest"
  pullPolicy: IfNotPresent
  pullSecrets:
    - name: regcred

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  className: nginx
  host: ""
  annotations: {}
    # kubernetes.io/tls-acme: "true"
  tls: []
    # - secretName: workshop-tls
    #   hosts:
    #     - workshop.example.com

config:
  title: "NKE Workshop"
  theme_color: "#1a73e8"
  message: "Welcome to the NKE Kubernetes Workshop!"

persistence:
  enabled: true
  storageClass: ""
  accessMode: ReadWriteOnce
  size: 1Gi

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    memory: 128Mi

serviceAccount:
  create: true
  name: ""
